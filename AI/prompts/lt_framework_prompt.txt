ЗАДАЧА: Исчерпывающий анализ метрик инструмента нагрузочного тестирования (lt_framework) в ходе теста.

ИСТОЧНИКИ ДАННЫХ (могут быть частично доступны):
- RPS по URL и имени транзакции (например, из метрики http_reqs), в разрезе group/name, дискрет 1s.
- Проверки в секунду (checks) в разрезе group/check.
- 95‑й перцентиль времени отклика (p95) по URL и имени транзакции (group/name), c агрегированием по окнам.
- Общий RPS по всем группам.

РЕКОМЕНДУЕМЫЕ ОРИЕНТИРЫ:
- Время отклика (latency p95): норма < 2000 мс; предупреждение 2000–5000 мс; критично > 5000 мс.
- Стабильность генератора: отсутствие систематических провалов RPS при неизменной цели нагрузки; минимальные очереди/ретраи.
- Число ошибок в секунду (checks): норма < 10 ; предупреждение 10–50 ; критично > 50
- Суммарный RPS для выявления максимальной производительности : норма > 1000 RPS; предупреждение > 750 RPS; критично > 500 RPS


ТРЕБОВАНИЯ К АНАЛИЗУ:
1) Проанализировать ВСЕ доступные метрики lt_framework:
   - RPS по (group, name) с точными временами пиков/провалов.
   - checks/second по (group, check) с акцентом на негативные проверки.
   - p95 latency по (group, name), все интервалы выхода за рекомендуемые пороги.
2) Для каждого (group, name) указать:
   - Полный диапазон значений (мин/макс/среднее) RPS и p95.
   - Временные интервалы превышения SLA по p95 и соответствующие значения RPS.
   - Временные окна пиков RPS и поведение latency вокруг пиков.
3) Корреляции:
   - Связь нагрузки и латентности: случаи роста p95 при росте RPS.
   - Связь проверок (checks) с деградациями (если корреляция наблюдается по времени).
4) Пиковая производительность / стабильность:
   - Если test_profile.mode="stability", НЕ определяйте peak_performance и НЕ ищите максимальный RPS.
     Оцените удержание заданной нагрузки, просадки RPS, checks/errors, latency и признаки накопления деградации.
   - Если это capacity/step тест и по данным можно агрегировать суммарный RPS по всем (group, name), определите peak_performance: {max_rps, max_time, drop_time, method="max_step_before_drop"}.
5) Вывод о поведении генератора:
   - Есть ли признаки насыщения на стороне генератора (просадки RPS при неизменной целевой нагрузке, очереди, ретраи и т.п.)?
   - Есть ли свидетельства, что ограничение на стороне SUT (рост p95/ошибок при росте RPS)?

СТРУКТУРА ОТВЕТА (СТРОГО НА РУССКОМ ЯЗЫКЕ):
- Верните СТРОГО JSON со схемой: {verdict, verdict_rationale, confidence, findings[], recommended_actions[], affected_components? , peak_performance? }.
- Поле verdict ДОЛЖНО принимать одно из значений: "Успешно" | "Есть риски" | "Провал" | "Недостаточно данных".
- Поле verdict_rationale ДОЛЖНО кратко объяснять вердикт: сначала 1-2 предложения с итогом, затем 2-4 ключевых фактора списком.
- confidence ∈ [0,1].
- findings[]:
  - Для каждого существенного наблюдения дайте объект {id, summary, severity, component, evidence}.
  - id должен быть коротким ASCII-идентификатором вроде f1, f2.
  - severity ∈ {critical|high|medium|low}.
  - component — укажите разумный идентификатор (например, "lt_framework" или конкретный (group/name) прицел).
  - evidence — краткая ссылка на метрику/порог и временной интервал, например: "RPS(group=A,name=/api/foo) peak at 12:31:00Z, p95=620мс 12:31–12:33Z".
- recommended_actions[]:
  - Каждый элемент должен быть объектом {summary, details, priority, affected_components[], for_finding_ids[]}.
  - for_finding_ids должен ссылаться на один или несколько id из findings.
  - details обязателен: опишите, что именно менять, зачем это нужно и как проверить, что деградация ушла.
  - Конкретные, проверяемые шаги: какие метрики/пороговые значения/конфигурации изменить и как верифицировать.
- peak_performance (опционально):
  - {max_rps, max_time, drop_time, method="max_step_before_drop"} — заполняйте только для capacity/step тестов при наличии достаточных данных.
  - Для stability/soak тестов не заполняйте peak_performance; выводы формулируйте через стабильность под нагрузкой.

ВАЖНО:
1) НЕ ПРИДУМЫВАТЬ ДАННЫХ — используйте только то, что явно вытекает из контекста.
2) Если данных недостаточно, верните verdict="Недостаточно данных" и перечислите пробелы в findings.
3) Если наблюдается деградация p95 при росте RPS, обязательно укажите интервалы и ориентиры SLA.
4) Для checks укажите отрицательные тенденции (рост негативных проверок/ошибок), если они наблюдаются.

Контекст ниже:
{{DATA_CONTEXT}}

