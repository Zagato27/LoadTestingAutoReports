ЗАДАЧА: Исчерпывающий отчет по всем JVM-метрикам  в ходе нагрузочного тестирования

ИСХОДНЫЕ ДАННЫЕ:
- Полные данные по использованию памяти (heap/nonheap)
- Полные данные по сборке мусора (gc_pause_seconds, action, cause)
- Полные данные по CPU usage (процесс, instance)
- Полные данные по потокам (live, daemon, peak)
- Полные данные по загруженным классам


РЕКОМЕНДУЕМЫЕ SLA (по мировой практике):
- Использование Heap памяти:
  * Нормальное: < 75% от выделенного максимума
  * Предупреждение: 75-85% от выделенного максимума
  * Критическое: > 85% от выделенного максимума
- Сборка мусора (GC):
  * Нормальное: < 5% времени CPU тратится на GC
  * Предупреждение: 5-10% времени CPU тратится на GC
  * Критическое: > 10% времени CPU тратится на GC
- Паузы GC:
  * Нормальное: < 200 мс
  * Предупреждение: 200-500 мс
  * Критическое: > 500 мс
- Использование CPU:
  * Нормальное: < 70% от доступного
  * Предупреждение: 70-85% от доступного
  * Критическое: > 85% от доступного


ТРЕБОВАНИЯ К АНАЛИЗУ:
1. Проанализировать КАЖДУЮ метрику из таблиц данных без исключения
2. По каждой метрике предоставить:
   - Минимальное, максимальное и среднее значение
   - Временные интервалы значимых изменений
   - Сравнение между различными экземплярами/процессами
   - Соответствие рекомендуемым SLA

3. По блокам метрик представить:
   - Использование памяти: полный анализ heap и non-heap 
   - Сборка мусора: все паузы GC с указанием причин и действий
   - CPU: полное использование по всем процессам
   - Потоки: полный анализ всех типов потоков
   - Классы: детальный анализ динамики загрузки

СТРУКТУРА ОТВЕТА:
1. Использование памяти
   • Исчерпывающий анализ всех метрик heap и non-heap
   • Все зафиксированные значения, включая минимумы и максимумы
   • Полная разбивка по process и instance
   • Сравнение с целевыми значениями SLA

2. Сборка мусора (GC)
   • Анализ всех метрик, связанных с GC
   • Полное перечисление причин и действий GC
   • Все данные по частоте и продолжительности
   • Соответствие целевым SLA по паузам и времени, затрачиваемому на GC

3. CPU и потоки
   • Детальный анализ всех CPU-метрик по каждому процессу
   • Полный анализ всех типов потоков
   • Исчерпывающее описание взаимосвязей между метриками
   • Сравнение с целевыми значениями SLA

4. Загруженные классы
   • Полный анализ динамики загрузки классов
   • Все значения и изменения для каждого процесса

5. Итоговые выводы
   • Обобщение по всем проанализированным метрикам
   • Выявление всех закономерностей и аномалий
   • Конкретные рекомендации на основе всех данных
   • Общая оценка соответствия SLA и рекомендации по оптимизации

ВАЖНО:
1. НЕ ПРИДУМЫВАТЬ ДАННЫХ. Если в исходных отчетах что-то не указано, отметить это как пробел в данных.
2. НЕ ДЕЛАТЬ ПРЕДПОЛОЖЕНИЙ о причинах и следствиях, если они не очевидны из числовых данных.
3. НЕ ВВОДИТЬ НОВЫХ ТЕРМИНОВ или метрик, которых нет в исходных отчетах.
4. При невозможности сделать вывод из-за недостаточности данных — честно указать на это.
5. Использовать только те числовые значения, которые явно приведены в исходных отчетах.
6. Включите в анализ абсолютно все значения из таблиц. Не опускайте данные, даже если они кажутся незначительными.


ФОРМАТ ОТВЕТА (ВСЕ ТЕКСТОВЫЕ ПОЛЯ — НА РУССКОМ):
- Отвечайте строго в JSON со схемой: {verdict, verdict_rationale, confidence, findings[], recommended_actions[]}.
- Поле verdict ДОЛЖНО принимать только одно из: "Успешно" | "Есть риски" | "Провал" | "Недостаточно данных".
- Поле verdict_rationale ДОЛЖНО кратко объяснять вердикт: сначала 1-2 предложения с итогом, затем 2-4 ключевых фактора списком.
- Каждый элемент findings ДОЛЖЕН содержать: id, summary, severity (critical/high/medium/low), component (имя сервиса/процесса/инстанса), evidence.
- id у findings должен быть коротким ASCII-идентификатором вроде f1, f2.
- Каждый элемент recommended_actions ДОЛЖЕН быть объектом {summary, details, priority, affected_components[], for_finding_ids[]}, где for_finding_ids ссылается на один или несколько id из findings.
- Поле details обязательно: 2-4 предложения с конкретными изменениями, ожидаемым эффектом и способом проверки.
- Если component не указан явно — извлеките его из evidence по лейблам application|service|job|pod|instance; если это невозможно, используйте "unknown".
- Если severity не указана или вне списка — используйте "low".
- Если данных недостаточно, верните verdict: "Недостаточно данных" и укажите причину в findings.
- Для каждого findings используйте фиксированные поля start_time, end_time, peak_time, evidence_summary и evidence_items[].
- Каждый элемент evidence_items должен быть объектом {metric, observed_value, threshold, note}.

ПРИМЕР ОТВЕТА (JSON):
```json
{
  "verdict": "Есть риски",
  "verdict_rationale": "JVM в целом работала стабильно, но в пиковом окне появились признаки давления по памяти и GC.\n- P99 GC pause заметно вырос.\n- Heap usage держался выше безопасного уровня.\n- Риски локализованы на нескольких инстансах billing.",
  "confidence": 0.86,
  "findings": [
    {"id": "f1", "summary": "P99 GC pause вырос до 620мс", "severity": "high", "component": "billing", "evidence": "application=billing, instance=10.0.1.15, 12:40–12:50, peak_time=12:47"},
    {"id": "f2", "summary": "Heap usage стабильно >85% на нескольких инстансах в окне пиков", "severity": "medium", "component": "billing", "evidence": "application=billing, 12:40–12:50, peak_time=12:46"}
  ],
  "recommended_actions": [
    {"summary": "Увеличить -Xmx/-Xms на billing до 4ГБ и пересобрать", "details": "Поднимите heap для сервиса billing симметрично по Xms/Xmx, чтобы сократить частоту GC и убрать давление на память в пиковое окно. После деплоя повторите тест и сравните паузы GC, p95 latency и частоту ошибок с исходным прогоном.", "priority": "high", "affected_components": ["billing"], "for_finding_ids": ["f1", "f2"]},
    {"summary": "Понизить RPS на /pay до 200 до фикса GC настроек", "details": "Если быстрый тюнинг JVM невозможен, временно ограничьте нагрузку на проблемный endpoint `/pay`, чтобы не доводить сервис до нестабильной зоны. Зафиксируйте безопасный уровень нагрузки и убедитесь, что на нем latency и error rate возвращаются в приемлемый диапазон.", "priority": "medium", "affected_components": ["billing"], "for_finding_ids": ["f1"]}
  ]
}
```